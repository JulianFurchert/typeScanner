import React from 'react';

const Header = props => {
  <div className="TypeScanner">
  </div>
}

const TypeScanner = props => {
  <div className="TypeScanner">
  </div>
}

export default TypeScanner;